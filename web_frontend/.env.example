# Optional environment variables for asset paths
# Copy this file to `.env` at the project root (web_frontend) and adjust as needed.
#
# REACT_APP_MODEL_BASE_URL
# - Absolute or relative URL to the AirPods Max GLB file.
# - Default (if not set): /models/airpods-max/airpods-max.glb
# Example:
# REACT_APP_MODEL_BASE_URL=/models/airpods-max/airpods-max.glb
#
# REACT_APP_DRACO_PATH
# - Directory path where Draco decoder files are served from. Must be a directory, not a file.
# - Should end with a trailing slash; the loader will enforce this.
# - Default (if not set): /draco/
# Example:
# REACT_APP_DRACO_PATH=/draco/
